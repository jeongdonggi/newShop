spring:
  application:
    name: newshop

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true # ?? system.out?? ??? ?? ??
        show_sql: true
        format_sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/jpashop
    username: root
    password: dkflfkd123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace # 물음표로 나오는 값을 로그로 보여줌
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # 파라미터 바인딩 값 출력
